name: renralski

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: mvn clean package -DskipTests
        env:
          MAVEN_OPTS: '-Dmaven.wagon.http.retryHandler.count=3'

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: enessaid2701
          password: EnesSaid-2701.

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: enessaid2701/renralski:latest

      - name: Copy files to server
        uses: appleboy/scp-action@master
        with:
          host: 178.157.15.185
          username: root
          password: 4B7SWKpt6NkaFym
          port: 23422
          source: "docker-compose.yml,insert_products.sql,insert_user.sql"
          target: "/root/renralski"

      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: 178.157.15.185
          username: root
          password: 4B7SWKpt6NkaFym
          port: 23422
          script: |
            cd /root/renralski || exit 1
            if [ -n "enessaid2701" ] && [ -n "EnesSaid-2701." ]; then
              echo "EnesSaid-2701." | docker login --username enessaid2701 --password-stdin
            fi
            docker-compose down --remove-orphans --rmi local || true
            docker-compose pull
            docker-compose up -d --force-recreate

